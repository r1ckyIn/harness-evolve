---
phase: 10-npm-package-ci-cd-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/publish.yml
  - README.md
  - tests/unit/readme-badges.test.ts
autonomous: true
requirements: [CIC-01, CIC-02, CIC-03]

must_haves:
  truths:
    - "Pushing to main or opening a PR to main triggers CI workflow that runs build, test, and typecheck"
    - "Pushing a v* tag triggers publish workflow that builds, tests, typechecks, and publishes to npm via OIDC"
    - "README.md displays dynamic npm version badge, CI status badge, and license badge with shields.io flat-square style"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI quality gate workflow"
      contains: "npm run build"
    - path: ".github/workflows/publish.yml"
      provides: "Automated npm publish workflow"
      contains: "npm publish --provenance"
    - path: "README.md"
      provides: "Dynamic badges for npm, CI, and license"
      contains: "img.shields.io/npm/v/harness-evolve"
    - path: "tests/unit/readme-badges.test.ts"
      provides: "Badge validation tests"
      contains: "describe"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts"
      via: "npm run commands"
      pattern: "npm run build"
    - from: ".github/workflows/publish.yml"
      to: "npm registry"
      via: "npm publish --provenance"
      pattern: "npm publish --provenance"
    - from: "README.md"
      to: ".github/workflows/ci.yml"
      via: "badge URL referencing workflow filename"
      pattern: "ci.yml"
---

<objective>
Create GitHub Actions CI/CD workflows and update README badges to dynamic shields.io badges.

Purpose: Automate quality gates (build + test + typecheck on every push/PR) and enable automated npm publishing via OIDC trusted publishing on version tag push. Replace static test badge with dynamic badges.
Output: .github/workflows/ci.yml, .github/workflows/publish.yml, updated README.md with dynamic badges, readme-badges test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-npm-package-ci-cd-pipeline/10-RESEARCH.md

<interfaces>
<!-- From 10-RESEARCH.md: Exact CI workflow YAML -->
CI workflow name must be: "CI"
CI workflow file must be: ci.yml (referenced by README badge URL)
Publish workflow name must be: "Publish"
Publish workflow file must be: publish.yml

<!-- From README.md: Current badge block (lines 3-8) to be replaced -->
```markdown
[![Language](https://img.shields.io/badge/Language-TypeScript-3178C6?style=flat-square&logo=typescript)](https://typescriptlang.org)
[![Runtime](https://img.shields.io/badge/Runtime-Node.js_22-339933?style=flat-square&logo=node.js)](https://nodejs.org)
[![License](https://img.shields.io/badge/License-MIT-blue?style=flat-square)](LICENSE)
[![Tests](https://img.shields.io/badge/Tests-336_passing-brightgreen?style=flat-square)]()
```

<!-- Target badge block (CIC-03): -->
```markdown
[![npm version](https://img.shields.io/npm/v/harness-evolve?style=flat-square)](https://www.npmjs.com/package/harness-evolve)
[![CI](https://img.shields.io/github/actions/workflow/status/r1ckyIn/harness-evolve/ci.yml?branch=main&style=flat-square&label=CI)](https://github.com/r1ckyIn/harness-evolve/actions/workflows/ci.yml)
[![License](https://img.shields.io/badge/License-MIT-blue?style=flat-square)](LICENSE)
[![Language](https://img.shields.io/badge/Language-TypeScript-3178C6?style=flat-square&logo=typescript)](https://typescriptlang.org)
[![Runtime](https://img.shields.io/badge/Runtime-Node.js_22-339933?style=flat-square&logo=node.js)](https://nodejs.org)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CI workflow, publish workflow, and README badge tests</name>
  <files>.github/workflows/ci.yml, .github/workflows/publish.yml, tests/unit/readme-badges.test.ts, README.md</files>
  <read_first>
    - README.md (current content to modify badge section)
    - .planning/phases/10-npm-package-ci-cd-pipeline/10-RESEARCH.md (exact workflow YAML and badge markdown)
  </read_first>
  <behavior>
    - Test: README.md contains `img.shields.io/npm/v/harness-evolve` (npm version badge)
    - Test: README.md contains `img.shields.io/github/actions/workflow/status/r1ckyIn/harness-evolve/ci.yml` (CI badge)
    - Test: README.md contains `img.shields.io/badge/License-MIT` (license badge)
    - Test: README.md does NOT contain `Tests-336_passing` (static badge removed)
  </behavior>
  <action>
    **Step 1: Create README badge test file `tests/unit/readme-badges.test.ts`:**

    Read `README.md` from the project root (use `import { readFileSync } from 'node:fs'` with path resolution via `fileURLToPath`).

    Assert:
    - `content` contains the string `img.shields.io/npm/v/harness-evolve`
    - `content` contains the string `img.shields.io/github/actions/workflow/status/r1ckyIn/harness-evolve/ci.yml`
    - `content` contains the string `img.shields.io/badge/License-MIT`
    - `content` does NOT contain the string `Tests-336_passing`
    - `content` contains `flat-square` (all badges use this style)

    **Step 2: Run test -- must FAIL (RED):**
    ```bash
    npx vitest run tests/unit/readme-badges.test.ts -x
    ```

    **Step 3: Create `.github/workflows/ci.yml`:**

    Create directories first: `mkdir -p .github/workflows`

    Write the file with this exact content:
    ```yaml
    name: CI

    on:
      push:
        branches: [main]
      pull_request:
        branches: [main]

    jobs:
      build:
        runs-on: ubuntu-latest
        strategy:
          matrix:
            node-version: [22]
        steps:
          - uses: actions/checkout@v4

          - uses: actions/setup-node@v4
            with:
              node-version: ${{ matrix.node-version }}
              cache: 'npm'

          - run: npm ci

          - run: npm run build

          - run: npm run typecheck

          - run: npm test

          - name: Validate package exports
            run: npx publint --strict

          - name: Validate TypeScript types
            run: npx @arethetypeswrong/cli --pack . --profile esm-only
    ```

    **Step 4: Create `.github/workflows/publish.yml`:**

    Write the file with this exact content:
    ```yaml
    name: Publish

    on:
      push:
        tags:
          - 'v*'

    jobs:
      publish:
        runs-on: ubuntu-latest
        permissions:
          contents: read
          id-token: write
        steps:
          - uses: actions/checkout@v4

          - uses: actions/setup-node@v4
            with:
              node-version: 22
              registry-url: 'https://registry.npmjs.org'

          - run: npm install -g npm@latest

          - run: npm ci

          - run: npm run build

          - run: npm test

          - run: npm run typecheck

          - name: Validate package exports
            run: npx publint --strict

          - run: npm publish --provenance
    ```

    **Step 5: Update README.md badge section:**

    Replace lines 4-8 (the 4 badge lines inside `<div align="center">`) with these 5 badge lines:
    ```markdown
    [![npm version](https://img.shields.io/npm/v/harness-evolve?style=flat-square)](https://www.npmjs.com/package/harness-evolve)
    [![CI](https://img.shields.io/github/actions/workflow/status/r1ckyIn/harness-evolve/ci.yml?branch=main&style=flat-square&label=CI)](https://github.com/r1ckyIn/harness-evolve/actions/workflows/ci.yml)
    [![License](https://img.shields.io/badge/License-MIT-blue?style=flat-square)](LICENSE)
    [![Language](https://img.shields.io/badge/Language-TypeScript-3178C6?style=flat-square&logo=typescript)](https://typescriptlang.org)
    [![Runtime](https://img.shields.io/badge/Runtime-Node.js_22-339933?style=flat-square&logo=node.js)](https://nodejs.org)
    ```

    This removes the static `Tests-336_passing` badge and adds dynamic npm version + CI status badges. The License, Language, and Runtime badges are kept.

    **Step 6: Run tests -- must PASS (GREEN):**
    ```bash
    npx vitest run tests/unit/readme-badges.test.ts -x
    ```

    **Step 7: Run full verification:**
    ```bash
    npx vitest run && npm run typecheck
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/unit/readme-badges.test.ts -x && npx vitest run && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists and contains `name: CI`
    - .github/workflows/ci.yml contains `push:` with `branches: [main]`
    - .github/workflows/ci.yml contains `pull_request:` with `branches: [main]`
    - .github/workflows/ci.yml contains `npm run build`
    - .github/workflows/ci.yml contains `npm run typecheck`
    - .github/workflows/ci.yml contains `npm test`
    - .github/workflows/ci.yml contains `npx publint --strict`
    - .github/workflows/ci.yml contains `npx @arethetypeswrong/cli --pack . --profile esm-only`
    - .github/workflows/publish.yml exists and contains `name: Publish`
    - .github/workflows/publish.yml contains `tags:` with `- 'v*'`
    - .github/workflows/publish.yml contains `id-token: write`
    - .github/workflows/publish.yml contains `registry-url: 'https://registry.npmjs.org'`
    - .github/workflows/publish.yml contains `npm install -g npm@latest`
    - .github/workflows/publish.yml contains `npm publish --provenance`
    - .github/workflows/publish.yml contains `npx publint --strict`
    - README.md contains `img.shields.io/npm/v/harness-evolve`
    - README.md contains `img.shields.io/github/actions/workflow/status/r1ckyIn/harness-evolve/ci.yml`
    - README.md contains `img.shields.io/badge/License-MIT`
    - README.md does NOT contain `Tests-336_passing`
    - tests/unit/readme-badges.test.ts exists and passes
    - All existing tests still pass
  </acceptance_criteria>
  <done>CI workflow runs build+test+typecheck+publint+attw on push/PR to main. Publish workflow runs on v* tag with OIDC provenance. README has dynamic npm version, CI status, and license badges. Static test count badge removed.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests pass (existing + readme-badges)
2. `npm run typecheck` -- no TypeScript errors
3. `.github/workflows/ci.yml` exists with correct triggers and steps
4. `.github/workflows/publish.yml` exists with OIDC permissions and v* tag trigger
5. README.md has dynamic badges (npm version, CI status, license)
6. README.md does NOT contain static `Tests-336_passing` badge
</verification>

<success_criteria>
- CI workflow file at .github/workflows/ci.yml triggers on push/PR to main, runs build + test + typecheck + publint + attw
- Publish workflow file at .github/workflows/publish.yml triggers on v* tag, uses OIDC with id-token:write, runs npm publish --provenance
- README.md has dynamic npm version badge, CI status badge, and license badge (shields.io flat-square)
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-npm-package-ci-cd-pipeline/10-02-SUMMARY.md`
</output>
