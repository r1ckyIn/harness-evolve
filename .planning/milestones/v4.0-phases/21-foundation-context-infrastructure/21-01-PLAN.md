---
phase: 21-foundation-context-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scan/schemas.ts
  - src/scan/context-builder.ts
  - tests/unit/scan/context-builder.test.ts
  - tests/integration/scan-pipeline-v4.test.ts
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "Nested hooks format {matcher, hooks: [{type, command}]} is parsed into individual hook entries"
    - "Flat hooks format {type, command} still works (backward compatibility)"
    - "Matcher field is extracted from nested format and available in hooks_registered"
    - "Real user settings.json with nested hooks produces zero false positives"
  artifacts:
    - path: "src/scan/schemas.ts"
      provides: "hooks_registered schema with optional matcher field"
      contains: "matcher: z.string().optional()"
    - path: "src/scan/context-builder.ts"
      provides: "Fixed extractHooksFromAllSettings supporting nested + flat formats"
      contains: "matcherGroup.hooks"
    - path: "tests/unit/scan/context-builder.test.ts"
      provides: "Unit tests for nested hooks, flat hooks, mixed format, matcher extraction"
      contains: "nested hooks"
    - path: "tests/integration/scan-pipeline-v4.test.ts"
      provides: "Integration test asserting INFRA-01 fix with concrete assertions"
      contains: "matcher"
  key_links:
    - from: "src/scan/context-builder.ts"
      to: "src/scan/schemas.ts"
      via: "ScanContext type includes matcher field in hooks_registered"
      pattern: "matcher"
    - from: "tests/unit/scan/context-builder.test.ts"
      to: "src/scan/context-builder.ts"
      via: "buildScanContext with nested hooks settings"
      pattern: "buildScanContext"
---

<objective>
Fix the nested hooks parsing bug in extractHooksFromAllSettings and add matcher field to the hooks_registered schema.

Purpose: INFRA-01 requires zero false positives when scanning real Claude Code hook configurations. The current parser reads top-level `type`/`command` from each array element, but the official hooks format nests actual hook definitions inside a `hooks` sub-array under matcher groups.

Output: Fixed parser, updated schema, comprehensive tests covering nested, flat, mixed formats and matcher extraction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-foundation-context-infrastructure/21-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/scan/schemas.ts:
```typescript
export const scanContextSchema = z.object({
  // ...
  hooks_registered: z.array(
    z.object({
      event: z.string(),
      scope: z.enum(['user', 'project', 'local']),
      type: z.string(),
      command: z.string().optional(),
      // MISSING: matcher field -- must be added
    }),
  ),
});
export type ScanContext = z.infer<typeof scanContextSchema>;
```

From src/scan/context-builder.ts (lines 266-298 -- the buggy function):
```typescript
function extractHooksFromAllSettings(
  settings: ScanContext['settings'],
): ScanContext['hooks_registered'] {
  // BUG: reads def.type and def.command directly from each array element
  // but Claude Code nested format has {matcher, hooks: [{type, command}]}
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add matcher field to schema and write tests for nested hooks parsing</name>
  <files>src/scan/schemas.ts, tests/unit/scan/context-builder.test.ts, tests/integration/scan-pipeline-v4.test.ts</files>
  <read_first>
    - src/scan/schemas.ts (current hooks_registered schema -- line 45-53)
    - tests/unit/scan/context-builder.test.ts (existing hooks tests -- line 104-138)
    - tests/integration/scan-pipeline-v4.test.ts (INFRA-01 placeholder test -- line 107-143)
    - src/scan/context-builder.ts (extractHooksFromAllSettings -- line 266-298)
  </read_first>
  <behavior>
    - Test: nested hooks format `{matcher: "Bash", hooks: [{type: "command", command: "echo test"}]}` produces one hook entry with event, scope, type="command", command="echo test", matcher="Bash"
    - Test: flat hooks format `{type: "command", command: "node capture.js"}` still produces one hook entry with type="command", command="node capture.js", matcher=undefined
    - Test: mixed format (nested + flat in different events) produces correct entries for both
    - Test: nested format without matcher field `{hooks: [{type: "command", command: "echo"}]}` produces hook entry with matcher=undefined
    - Test: nested format with multiple inner hooks `{matcher: "*", hooks: [{...}, {...}]}` produces multiple hook entries each with matcher="*"
    - Integration test: buildScanContext with nested hooks settings produces hooks_registered entries with correct command and matcher values (update existing INFRA-01 placeholder)
  </behavior>
  <action>
    1. In `src/scan/schemas.ts`, add `matcher: z.string().optional()` to the hooks_registered z.object definition, after the `command` field (line 50). The resulting schema entry becomes:
    ```typescript
    hooks_registered: z.array(
      z.object({
        event: z.string(),
        scope: z.enum(['user', 'project', 'local']),
        type: z.string(),
        command: z.string().optional(),
        matcher: z.string().optional(),
      }),
    ),
    ```

    2. In `tests/unit/scan/context-builder.test.ts`, add a new `describe('nested hooks parsing (INFRA-01)')` block after the existing `extracts hooks_registered from all settings scopes` test (after line 138). Add tests for each behavior listed above. Use project-scope settings.json (`join(fakeCwd, '.claude', 'settings.json')`) with nested format hooks. Assert specific values: `expect(hook.matcher).toBe('Bash')`, `expect(hook.command).toBe('echo test')`, etc.

    3. In `tests/integration/scan-pipeline-v4.test.ts`, update the existing INFRA-01 test (line 107-143) to add concrete assertions:
    - Assert `parsed.hooks_registered[0].command` equals `'echo nested-hook'` (not just length check)
    - Assert `parsed.hooks_registered[0].matcher` equals `'Bash'`
    - Assert `parsed.hooks_registered[0].type` equals `'command'`

    4. Run tests -- they MUST FAIL (RED phase) because extractHooksFromAllSettings has not been fixed yet. The nested format tests will fail because the current parser reads `def.type` directly instead of drilling into `def.hooks[].type`.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/scan/context-builder.test.ts tests/integration/scan-pipeline-v4.test.ts 2>&1 | grep -E "(FAIL|PASS|Tests)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/scan/schemas.ts contains `matcher: z.string().optional()` inside hooks_registered z.object
    - tests/unit/scan/context-builder.test.ts contains `describe('nested hooks parsing (INFRA-01)'`
    - tests/unit/scan/context-builder.test.ts contains at least 5 test cases for nested/flat/mixed/no-matcher/multiple-inner
    - tests/integration/scan-pipeline-v4.test.ts contains `expect(parsed.hooks_registered[0].matcher).toBe('Bash')`
    - New tests FAIL because implementation is not yet fixed (TDD RED phase)
  </acceptance_criteria>
  <done>Schema has optional matcher field, all nested hooks tests written, tests fail as expected (RED)</done>
</task>

<task type="auto">
  <name>Task 2: Fix extractHooksFromAllSettings to support nested format (GREEN)</name>
  <files>src/scan/context-builder.ts</files>
  <read_first>
    - src/scan/context-builder.ts (extractHooksFromAllSettings -- lines 266-298)
    - src/scan/schemas.ts (updated schema with matcher field from Task 1)
    - tests/unit/scan/context-builder.test.ts (new nested hooks tests from Task 1)
  </read_first>
  <action>
    Replace the `extractHooksFromAllSettings` function body (lines 266-298 in src/scan/context-builder.ts) with the following logic:

    For each `def` in the event's array:
    1. Check if `def` has a `hooks` sub-array (`Array.isArray(matcherGroup.hooks)`)
    2. If YES (nested format): extract `matcher` from the outer object (`typeof matcherGroup.matcher === 'string' ? matcherGroup.matcher : undefined`), then iterate each inner hook in `matcherGroup.hooks` and push `{event, scope, type: String(h.type ?? 'command'), command: typeof h.command === 'string' ? h.command : undefined, matcher}`
    3. If NO (flat format fallback): push `{event, scope, type: String(matcherGroup.type ?? 'command'), command: typeof matcherGroup.command === 'string' ? matcherGroup.command : undefined}` -- no matcher field (backward compat)

    The exact implementation from the RESEARCH.md code example (lines 240-296) should be used. Key changes from current code:
    - Rename `hookDef` to `matcherGroup` for clarity
    - Add `Array.isArray(matcherGroup.hooks)` check as the branching condition
    - In nested branch: loop inner hooks, extract matcher from outer
    - In flat branch: same as current code but without matcher field

    After fixing, run the full test suite. All tests MUST pass including the new nested hooks tests from Task 1 AND the existing flat-format test at line 104-138.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/scan/context-builder.test.ts tests/integration/scan-pipeline-v4.test.ts && echo "ALL PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/scan/context-builder.ts contains `Array.isArray(matcherGroup.hooks)` in extractHooksFromAllSettings
    - src/scan/context-builder.ts contains `matcher` in the hooks.push call within the nested branch
    - Existing test "extracts hooks_registered from all settings scopes" (flat format) still passes
    - All new nested hooks tests pass (GREEN phase)
    - Integration test INFRA-01 assertions pass (matcher='Bash', command='echo nested-hook')
    - Full test suite: `npx vitest run` exits 0
  </acceptance_criteria>
  <done>extractHooksFromAllSettings correctly parses both nested and flat hook formats, matcher field extracted, all tests green</done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- full test suite passes (all 654+ tests)
2. `npx tsc --noEmit` -- no type errors
3. `npm run build` -- build succeeds
4. Schema change is backward compatible (optional field)
</verification>

<success_criteria>
- Nested hooks `{matcher, hooks: [{type, command}]}` parsed into individual hook entries with matcher field
- Flat hooks `{type, command}` still work (backward compat, existing test passes)
- Zero false positives on nested format (integration test with real-world-like config)
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-foundation-context-infrastructure/21-01-SUMMARY.md`
</output>
