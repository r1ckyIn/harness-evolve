---
status: partial
phase: v2.0-milestone
source: [all SUMMARY.md files across phases 1-16]
started: 2026-04-04T11:00:00.000Z
updated: 2026-04-04T11:07:30.000Z
---

## Current Test

[testing paused — 1 item blocked (npm first publish), 1 deferred (live session)]

## Tests

### 1. Build & Type Safety
expected: `npx tsup` produces 9 ESM entry points + DTS declarations with zero errors. `npx tsc --noEmit` reports zero type errors.
result: pass

### 2. Full Test Suite
expected: `npx vitest run` reports 607 tests passing across 58 test files with zero failures.
result: pass

### 3. CLI Help
expected: `harness-evolve --help` shows 8 commands: init, status, uninstall, scan, pending, apply-one, dismiss, help.
result: pass

### 4. CLI Status
expected: `harness-evolve status` shows interaction count, last analysis time, pending recommendation count, and hook registration status.
result: pass

### 5. CLI Init — Hook Registration
expected: `harness-evolve init --yes` registers 6 hooks (UserPromptSubmit, PreToolUse, PostToolUse, PostToolUseFailure, PermissionRequest, Stop) in `~/.claude/settings.json` with backup created.
result: pass

### 6. CLI Init — Hook Descriptions (Phase 16: UX-02)
expected: Init output shows each hook with description format: `EventName (async) -- Description text`. Example: `UserPromptSubmit -- Captures prompts and delivers optimization notifications`.
result: pass

### 7. CLI Init — Slash Command Installation (Phase 15: CMD-01)
expected: Init installs `/evolve:scan` and `/evolve:apply` into `.claude/commands/evolve/`. Existing files are skipped (create-only guard).
result: pass

### 8. CLI Init — Deep Scan (Phase 12: SCN-01)
expected: Init runs deep configuration scan after hook registration, reporting detected issues with confidence levels.
result: pass

### 9. CLI Scan
expected: `harness-evolve scan` outputs structured JSON with `generated_at`, `recommendation_count`, and `recommendations[]` array.
result: pass

### 10. CLI Pending
expected: `harness-evolve pending` outputs JSON with `pending[]` array and `count` field. Array is empty when no analysis has run.
result: pass

### 11. CLI Uninstall
expected: `harness-evolve uninstall --yes` removes hooks from settings.json (with backup) and removes slash command files. Reports each removed item.
result: pass

### 12. UserPromptSubmit Hook — Log Capture (Phase 2: COL-01)
expected: Sending valid JSON via stdin creates/appends to `~/.harness-evolve/logs/prompts/YYYY-MM-DD.jsonl` with timestamp, session_id, prompt, and prompt_length.
result: pass

### 13. PreToolUse Hook — Tool Tracking (Phase 2: COL-03)
expected: Sending valid JSON creates/appends to `~/.harness-evolve/logs/tools/YYYY-MM-DD.jsonl` with tool_name and tool_input.
result: pass

### 14. PermissionRequest Hook — Permission Logging (Phase 2: COL-02)
expected: Sending valid JSON creates/appends to `~/.harness-evolve/logs/permissions/YYYY-MM-DD.jsonl` with tool_name and action.
result: pass

### 15. Counter Increment (Phase 1: FND-01)
expected: Each UserPromptSubmit hook call increments `~/.harness-evolve/counter.json` total by 1 and tracks per-session counts. After 3 calls with session_id "smoke-test", counter shows `"smoke-test": 3`.
result: pass

### 16. Stop Hook — Threshold Check (Phase 4: ANL-01)
expected: Stop hook checks counter against threshold (default 50). When under threshold, exits cleanly without triggering analysis. When at/above threshold, triggers pattern analysis.
result: pass

### 17. Notification Injection — Plural (Phase 16: UX-01)
expected: When notification flag exists with count 5, next UserPromptSubmit outputs exactly: `[harness-evolve] 5 new suggestions found. Run /evolve:apply to review.`
result: pass

### 18. Notification Injection — Singular (Phase 16: UX-01)
expected: When notification flag exists with count 1, output uses singular: `[harness-evolve] 1 new suggestion found. Run /evolve:apply to review.`
result: pass

### 19. Notification Flag Cleared After Delivery (Phase 5: DEL-03)
expected: After notification is injected via stdout, the flag file at `~/.harness-evolve/analysis/has-pending-notifications` is deleted. Next UserPromptSubmit produces no notification.
result: pass

### 20. Input Validation — Silent Failure
expected: Sending malformed JSON (missing required fields like `cwd`, `permission_mode`, `hook_event_name`) to any hook exits with code 0 and produces no output. Hooks never block Claude Code.
result: pass

### 21. Real Claude Code Integration
expected: After `harness-evolve init`, hooks fire automatically during a real Claude Code session. Prompts are captured, tools are tracked, permissions are logged. Counter increments with each user prompt.
result: skipped
reason: "Hooks registered in settings.json (6/6 verified). Current session started before registration — need NEW session to verify live hook firing. Hooks themselves verified working via CLI stdin tests (tests 12-20)."

### 22. Analysis Trigger at Threshold
expected: After reaching 50 interactions, Stop hook triggers full pattern analysis pipeline. Analysis produces `~/.harness-evolve/analysis/analysis-result.json` with classified recommendations.
result: pass
note: "Counter set to 56 via 40 generated prompts + existing 16. Stop hook (stop_hook_active=false) triggered full pipeline: pre-processing (48 prompts, 3 tools, 5 permissions, 6 sessions) → environment discovery (Claude Code 2.1.92, 17 hooks) → 8 classifiers (23 patterns evaluated) → 2 LOW-confidence recommendations. analysis-result.json (1636 bytes), environment-snapshot.json (7591 bytes), summary.json (1721 bytes) all produced."

### 23. npm Publish Pipeline (Phase 10: PKG-04)
expected: Pushing a `v*` git tag triggers GitHub Actions publish workflow using OIDC trusted publishing to npm. Package appears on npmjs.com with correct metadata, exports map, and bin entry.
result: blocked
blocked_by: third-party
reason: "First publish must be manual (`npm publish`). OIDC trusted publishing requires initial npm package registration. CI/CD pipeline structure verified in .github/workflows/ (build+test+typecheck+publint+attw CI + OIDC publish on v* tag)."

## Summary

total: 23
passed: 21
issues: 0
pending: 0
skipped: 1
blocked: 1

## Gaps

[none]
