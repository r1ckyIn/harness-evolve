---
phase: 19.1-developer-full-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analysis/classifiers/onboarding.ts
  - src/scan/scanners/redundancy.ts
  - src/scan/scanners/staleness.ts
  - src/scan/scanners/mechanization.ts
autonomous: true
requirements:
  - INT-01

must_haves:
  truths:
    - "`npm run build` exits with code 0 (ESM + DTS both succeed)"
    - "`tsc --noEmit` exits with code 0 (zero type errors)"
    - "`npm test` passes with 708+ tests"
    - "All recommendation push calls include severity field"
  artifacts:
    - path: "src/analysis/classifiers/onboarding.ts"
      provides: "Onboarding classifier with severity field on all 4 recommendations"
      contains: "severity: 'suggestion'"
    - path: "src/scan/scanners/redundancy.ts"
      provides: "Redundancy scanner with severity on all pushes"
      contains: "severity: 'suggestion'"
    - path: "src/scan/scanners/staleness.ts"
      provides: "Staleness scanner with severity on all pushes"
      contains: "severity: 'suggestion'"
    - path: "src/scan/scanners/mechanization.ts"
      provides: "Mechanization scanner with severity on all pushes"
      contains: "severity: 'suggestion'"
  key_links:
    - from: "src/schemas/recommendation.ts"
      to: "src/analysis/classifiers/onboarding.ts"
      via: "Recommendation type requires severity field"
      pattern: "severity: severitySchema"
    - from: "src/schemas/recommendation.ts"
      to: "src/scan/scanners/*.ts"
      via: "ScanRecommendation uses same Recommendation type"
      pattern: "severity: 'suggestion'"
---

<objective>
Fix all TypeScript build errors and tech debt that block a clean build gate, so that subsequent integration testing plans can rely on `npm run build` and `tsc --noEmit` as pass/fail gates.

Purpose: Clean build is a prerequisite for all integration testing (per D-04, D-07). The DTS build error in onboarding classifiers and missing severity fields in 3 pre-Phase-18 scanners must be resolved first.
Output: Clean `npm run build` (exit 0), clean `tsc --noEmit` (exit 0), all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v3.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add severity field to onboarding classifier and pre-Phase-18 scanners</name>
  <files>src/analysis/classifiers/onboarding.ts, src/scan/scanners/redundancy.ts, src/scan/scanners/staleness.ts, src/scan/scanners/mechanization.ts</files>
  <read_first>
    - src/schemas/recommendation.ts (line 60: severity field definition -- `severity: severitySchema.optional().default('suggestion')`)
    - src/analysis/classifiers/onboarding.ts (lines 35, 56, 77, 95: four recommendations.push() calls missing severity)
    - src/scan/scanners/redundancy.ts (lines 34, 70: two recommendations.push() calls missing severity)
    - src/scan/scanners/staleness.ts (lines 74, 102: two recommendations.push() calls missing severity)
    - src/scan/scanners/mechanization.ts (line 67: one recommendations.push() call missing severity)
  </read_first>
  <action>
Add `severity: 'suggestion'` to every `recommendations.push({...})` call in these 4 files:

**src/analysis/classifiers/onboarding.ts** -- 4 locations:
- Line 35 (hooks recommendation): add `severity: 'suggestion',` after `pattern_type: 'onboarding_start_hooks',`
- Line 56 (rules recommendation): add `severity: 'suggestion',` after `pattern_type: 'onboarding_start_rules',`
- Line 77 (CLAUDE.md recommendation): add `severity: 'suggestion',` after `pattern_type: 'onboarding_start_claudemd',`
- Line 95 (power user recommendation): add `severity: 'suggestion',` after `pattern_type: 'onboarding_optimize',`

**src/scan/scanners/redundancy.ts** -- 2 locations:
- Line 34 push: add `severity: 'suggestion',` field
- Line 70 push: add `severity: 'suggestion',` field

**src/scan/scanners/staleness.ts** -- 2 locations:
- Line 74 push: add `severity: 'suggestion',` field
- Line 102 push: add `severity: 'suggestion',` field

**src/scan/scanners/mechanization.ts** -- 1 location:
- Line 67 push: add `severity: 'suggestion',` field

Place `severity: 'suggestion'` consistently after the `pattern_type` field in each push object for readability.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/harness-evolve && npm run build 2>&1 | tail -5 | grep -q "Build success" && echo "BUILD OK" || echo "BUILD FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "severity: 'suggestion'" src/analysis/classifiers/onboarding.ts` returns 4
    - `grep -c "severity: 'suggestion'" src/scan/scanners/redundancy.ts` returns 2
    - `grep -c "severity: 'suggestion'" src/scan/scanners/staleness.ts` returns 2
    - `grep -c "severity: 'suggestion'" src/scan/scanners/mechanization.ts` returns 1
    - `npm run build` exits with code 0 (both ESM and DTS succeed)
    - `tsc --noEmit` exits with code 0
    - `npm test` passes with 708+ tests, exit code 0
  </acceptance_criteria>
  <done>All 9 severity fields added, `npm run build` exits 0, `tsc --noEmit` exits 0, `npm test` passes 708+ tests.</done>
</task>

</tasks>

<verification>
Run full verification loop:
1. `npm run build` -- must exit 0 with "Build success" for both ESM and DTS
2. `tsc --noEmit` -- must exit 0
3. `npm test` -- must pass 708+ tests
4. `grep -rc "severity: 'suggestion'" src/analysis/classifiers/onboarding.ts src/scan/scanners/redundancy.ts src/scan/scanners/staleness.ts src/scan/scanners/mechanization.ts` -- must total 9
</verification>

<success_criteria>
- `npm run build` exits 0 (DTS error resolved)
- `tsc --noEmit` exits 0 (zero type errors)
- `npm test` exits 0 (all tests pass)
- All 4 files have severity field on every recommendation push
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-developer-full-integration-testing/19.1-01-SUMMARY.md`
</output>
