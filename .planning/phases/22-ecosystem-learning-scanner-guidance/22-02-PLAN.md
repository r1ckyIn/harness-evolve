---
phase: 22-ecosystem-learning-scanner-guidance
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/commands/evolve-apply.ts
  - tests/unit/commands/templates.test.ts
autonomous: false
requirements: [SCAN-01, SCAN-02]

must_haves:
  truths:
    - "/evolve:apply template references model-generated findings from /evolve:scan"
    - "Apply template context section explains findings come from model analysis via store-findings"
    - "Apply template version is bumped to 4 to stay in sync with scan template"
    - "Live /evolve:scan invocation in Claude Code produces structured findings"
  artifacts:
    - path: "src/commands/evolve-apply.ts"
      provides: "Updated apply template (v4) aligned with model-driven pipeline"
      contains: "APPLY_TEMPLATE_VERSION = '4'"
    - path: "tests/unit/commands/templates.test.ts"
      provides: "Updated apply template tests for v4"
      contains: "template-version: 4"
  key_links:
    - from: "src/commands/evolve-apply.ts"
      to: "src/commands/evolve-scan.ts"
      via: "template cross-reference (/evolve:scan)"
      pattern: "/evolve:scan"
---

<objective>
Update the /evolve:apply template to align with model-driven findings pipeline and verify the complete scan-to-apply flow works in a live Claude Code session.

Purpose: The apply template (v3) still describes findings as coming from "harness-evolve scan or background analysis hooks." After Phase 22 Plan 01 rewrites the scan template to use model-driven analysis via scan-context + store-findings, the apply template needs minor alignment updates. A live verification checkpoint confirms the full pipeline works end-to-end.

Output: Updated `src/commands/evolve-apply.ts` (v4), updated apply tests, and human verification of live scan flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-ecosystem-learning-scanner-guidance/22-RESEARCH.md
@.planning/phases/22-ecosystem-learning-scanner-guidance/22-01-SUMMARY.md

<interfaces>
From src/commands/evolve-apply.ts (current v3, to be updated):
```typescript
const APPLY_TEMPLATE_VERSION = '3';
export function getApplyTemplateVersion(): string { return APPLY_TEMPLATE_VERSION; }
export function generateApplyCommand(): string { return `---\nname: apply\n...`; }
```

From src/commands/evolve-scan.ts (v4 after Plan 01):
- Template instructs model to run `scan-context`, analyze config, pipe to `store-findings`
- Findings use Recommendation schema with scan_* pattern_type values
- Model produces human-readable summary after storing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update apply template to align with model-driven pipeline</name>
  <files>src/commands/evolve-apply.ts, tests/unit/commands/templates.test.ts</files>
  <read_first>
    - src/commands/evolve-apply.ts (current v3 apply template)
    - src/commands/evolve-scan.ts (v4 scan template from Plan 01 -- understand the new pipeline)
    - tests/unit/commands/templates.test.ts (current tests for apply template)
  </read_first>
  <action>
    Make targeted updates to `src/commands/evolve-apply.ts`:

    1. **Version bump:** Change `APPLY_TEMPLATE_VERSION` from `'3'` to `'4'`.

    2. **Context section update** (line ~37-39 in current template): Change from:
       ```
       This command reads pending recommendations generated by `harness-evolve scan` or background analysis hooks.
       ```
       To:
       ```
       This command reads pending recommendations generated by `/evolve:scan` (model-driven analysis) or background analysis hooks. Findings are produced by the model analyzing your configuration and stored via the `store-findings` pipeline.
       ```

    3. **Notes section update** (last section): Change from:
       ```
       - Recommendations are generated by `/evolve:scan` or automatic background analysis hooks.
       ```
       To:
       ```
       - Recommendations are generated by `/evolve:scan` (model-driven configuration analysis) or automatic background analysis hooks.
       ```

    4. **Update apply template tests** in `tests/unit/commands/templates.test.ts`:
       - Change `has template version 3` to `has template version 4` for the apply template test
       - All other apply tests should still pass unchanged

    These are minor text updates -- no structural changes to the apply template.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/harness-evolve && npx vitest run tests/unit/commands/templates.test.ts && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/evolve-apply.ts contains `APPLY_TEMPLATE_VERSION = '4'`
    - src/commands/evolve-apply.ts template contains `model-driven analysis`
    - src/commands/evolve-apply.ts template contains `store-findings`
    - src/commands/evolve-apply.ts template contains `<!-- template-version: 4 -->`
    - tests/unit/commands/templates.test.ts apply tests check for `template-version: 4`
    - `npx vitest run tests/unit/commands/templates.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    Apply template updated to v4 with model-driven pipeline references. Context section explains findings come from model analysis via store-findings. All tests pass, build succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification of /evolve:scan model-driven flow</name>
  <files>src/commands/evolve-scan.ts, src/commands/evolve-apply.ts</files>
  <read_first>
    - src/commands/evolve-scan.ts (v4 template to verify in live session)
    - src/commands/evolve-apply.ts (v4 template to verify apply pipeline)
  </read_first>
  <action>
    Build the project and update slash command templates, then verify live model-driven scan flow:

    1. Build: `npm run build`
    2. Update templates: `npx harness-evolve init`
    3. Open a new Claude Code session in any project with a CLAUDE.md file
    4. Invoke `/evolve:scan` and observe model behavior
    5. Invoke `/evolve:apply` to verify findings are in the pipeline

    Present verification instructions to user and wait for approval.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/harness-evolve && npm run build && node -e "import('./dist/commands/evolve-scan.js').then(m => { const t = m.generateScanCommand(); console.log('v4:', t.includes('template-version: 4')); console.log('scan-context:', t.includes('scan-context')); console.log('7 areas:', ['Area 1','Area 2','Area 3','Area 4','Area 5','Area 6','Area 7'].every(a => t.includes(a))); })"</automated>
  </verify>
  <acceptance_criteria>
    - Built dist contains v4 scan template with scan-context reference and 7 analysis areas
    - User confirms /evolve:scan produces model-driven analysis in live Claude Code session
    - User confirms /evolve:apply shows stored findings from model scan
  </acceptance_criteria>
  <done>
    Live verification confirms the model-driven scan pipeline works end-to-end: scan-context gathers data, model analyzes using 7 area checklists, store-findings persists results, /evolve:apply shows stored findings. User approved.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- full suite passes with apply template v4 updates
2. `npx tsc --noEmit` -- clean
3. `npm run build` -- succeeds
4. Live `/evolve:scan` invocation produces model-driven analysis with structured findings
5. Live `/evolve:apply` shows findings stored by the scan
</verification>

<success_criteria>
1. Apply template (v4) references model-driven pipeline and store-findings
2. Both scan (v4) and apply (v4) templates are version-synchronized
3. All unit tests pass for both templates
4. Live verification confirms model executes scan guidance correctly: reads scan-context, produces findings, stores via store-findings, presents summary
</success_criteria>

<output>
After completion, create `.planning/phases/22-ecosystem-learning-scanner-guidance/22-02-SUMMARY.md`
</output>
