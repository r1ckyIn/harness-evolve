---
phase: 23-model-driven-validation-legacy-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
files_modified: []
autonomous: false
requirements:
  - MODEL-01
  - MODEL-02
  - MODEL-03
  - MODEL-04

must_haves:
  truths:
    - "Model detects semantic ESM vs CommonJS contradiction in test Config 1"
    - "Model identifies pytest vs npm test cross-file inconsistency in test Config 2"
    - "Model identifies hookable operations from natural language phrasing in test Config 3"
    - "Model produces findings for new guidance area added via .md edit in test Config 4"
  artifacts: []
  key_links: []
---

<objective>
Manually validate that model-driven scanning is at least as capable as old code-based scanners by running /evolve:scan against the 4 test config fixtures created in Plan 01.

Purpose: MODEL-01 through MODEL-04 cannot be tested with automated Vitest because they require live language model inference via a Claude Code session. This checkpoint verifies the model's semantic understanding capabilities that justify removing the legacy regex-based scanners.

Output: Human-verified sign-off on model capabilities
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-model-driven-validation-legacy-cleanup/23-RESEARCH.md
@.planning/phases/23-model-driven-validation-legacy-cleanup/23-01-SUMMARY.md
@.planning/phases/23-model-driven-validation-legacy-cleanup/23-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Validate model-driven scanning capabilities (MODEL-01 through MODEL-04)</name>
  <files></files>
  <action>
    This is a human verification checkpoint. The executor should present the following test instructions to the user and await sign-off.

    **Prerequisites:** Phase 21 and 22 must be complete (scan-context CLI working, guidance docs embedded in /evolve:scan template).

    **Test 1: Semantic Conflict Detection (MODEL-01)**
    1. Copy `tests/fixtures/model-validation/semantic-conflict/` to a temp directory
    2. From that directory, run `harness-evolve scan-context` to get the JSON context
    3. In a Claude Code session, invoke `/evolve:scan` (or feed the context to the model with scan guidance)
    4. Expected: Model identifies contradiction between "ES modules exclusively" (CLAUDE.md) and "module: commonjs" (rules/compatibility.md)
    5. Note: Old conflict.ts scanner would MISS this -- it only checks always/never, enable/disable, require/forbid keyword pairs

    **Test 2: Cross-File Inconsistency (MODEL-02)**
    1. Copy `tests/fixtures/model-validation/cross-file-inconsistency/` to a temp directory
    2. Run `harness-evolve scan-context`, then invoke `/evolve:scan`
    3. Expected: Model identifies that rules/testing.md says "run pytest" (Python) but settings.json hook runs "npm test" (JavaScript)
    4. Note: Old mechanization scanner would see hooks exist and suppress the finding

    **Test 3: Natural Language Hookable Operation (MODEL-03)**
    1. Copy `tests/fixtures/model-validation/natural-language-hookable/` to a temp directory
    2. Run `harness-evolve scan-context`, then invoke `/evolve:scan`
    3. Expected: Model identifies 3 hookable operations: "formatting verified automatically", "branch naming enforced on push", "type checking should never be skipped"
    4. Note: Old mechanization.ts 6 regex patterns would miss ALL of these phrasings

    **Test 4: Guidance Extensibility (MODEL-04)**
    1. Copy `tests/fixtures/model-validation/guidance-extensibility/` to a temp directory
    2. In the /evolve:scan template (or guidance doc), manually add an "Area 8: Documentation Coverage" section with checks for README.md presence
    3. Run `/evolve:scan` on the test config
    4. Expected: Model produces a finding about missing README.md
    5. Note: No TypeScript code was changed -- extensibility via .md editing alone

    **Resume signal:** User indicates PASS/FAIL for each test, or "all pass" if all succeed.
  </action>
  <verify>
    <automated>echo "Manual verification -- human sign-off required for MODEL-01 through MODEL-04"</automated>
  </verify>
  <done>All 4 MODEL-* requirements validated by human observation: MODEL-01 semantic conflict detected, MODEL-02 cross-file inconsistency detected, MODEL-03 natural language hookable operations detected, MODEL-04 guidance extensibility confirmed</done>
</task>

</tasks>

<verification>
All 4 MODEL-* requirements validated by human observation of live model output.
</verification>

<success_criteria>
- MODEL-01: Model detects semantic ESM vs CommonJS conflict (not keyword opposition)
- MODEL-02: Model identifies pytest vs npm test cross-file inconsistency
- MODEL-03: Model identifies hookable operations from natural language (not regex matches)
- MODEL-04: Adding guidance .md section produces new findings without code changes
- Human sign-off recorded
</success_criteria>

<output>
After completion, create `.planning/phases/23-model-driven-validation-legacy-cleanup/23-03-SUMMARY.md`
</output>
