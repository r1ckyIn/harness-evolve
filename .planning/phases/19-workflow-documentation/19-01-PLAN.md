---
phase: 19-workflow-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/evolve-scan.ts
  - src/commands/evolve-apply.ts
  - tests/unit/commands/templates.test.ts
autonomous: true
requirements: [WFL-01, WFL-02]

must_haves:
  truths:
    - "/evolve:scan slash command template contains comprehensive workflow sections including prerequisites, step-by-step instructions, output format specification, error handling, and edge cases"
    - "/evolve:apply slash command template contains comprehensive workflow sections including prerequisites, step-by-step instructions, output format specification, error handling, and edge cases"
    - "Both templates are fully self-contained with no references to CLAUDE.md preloading or external rule files"
    - "Templates include allowed-tools frontmatter to avoid unnecessary permission prompts for harness-evolve CLI calls"
  artifacts:
    - path: "src/commands/evolve-scan.ts"
      provides: "Comprehensive scan workflow template generator"
      contains: "Error Handling"
      min_lines: 100
    - path: "src/commands/evolve-apply.ts"
      provides: "Comprehensive apply workflow template generator"
      contains: "Error Handling"
      min_lines: 100
    - path: "tests/unit/commands/templates.test.ts"
      provides: "Tests verifying workflow completeness and self-containment"
      contains: "workflow"
  key_links:
    - from: "src/commands/evolve-scan.ts"
      to: "installed scan.md"
      via: "generateScanCommand() output"
      pattern: "## Prerequisites"
    - from: "src/commands/evolve-apply.ts"
      to: "installed apply.md"
      via: "generateApplyCommand() output"
      pattern: "## Prerequisites"
---

<objective>
Rewrite both slash command template generators (evolve-scan.ts and evolve-apply.ts) to produce comprehensive, GSD-style workflow documents that fully specify Claude's behavior when the command is invoked. Add version tracking constants for future update mechanism.

Purpose: Satisfy WFL-01 (each command has a complete workflow .md) and WFL-02 (workflow injected via command template, not CLAUDE.md). The current templates are ~75 lines with vague instructions that produce inconsistent Claude behavior. The new templates will be 100-150+ lines with explicit prerequisites, step-by-step instructions, exact output formatting, error handling, and edge case documentation.

Output: Two rewritten template generators + updated test suite verifying workflow completeness and self-containment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-workflow-documentation/19-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/commands/evolve-scan.ts:
```typescript
export function generateScanCommand(): string;
// Returns the full .md content for the /evolve:scan slash command
// Currently ~75 lines, to be expanded to 100-150+ lines
```

From src/commands/evolve-apply.ts:
```typescript
export function generateApplyCommand(): string;
// Returns the full .md content for the /evolve:apply slash command
// Currently ~78 lines, to be expanded to 100-150+ lines
```

From tests/unit/commands/templates.test.ts:
```typescript
// Currently 18 tests across 2 describe blocks
// Tests validate frontmatter, CLI commands, and basic content
// Needs expansion for workflow section completeness and self-containment
```

Verified frontmatter options (from Claude Code skills docs):
```yaml
name: scan
description: Short description
disable-model-invocation: true
argument-hint: "[optional args]"
allowed-tools: Bash(npx harness-evolve *)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite scan template generator with comprehensive workflow</name>
  <files>src/commands/evolve-scan.ts, tests/unit/commands/templates.test.ts</files>
  <read_first>
    - src/commands/evolve-scan.ts (current template to rewrite)
    - tests/unit/commands/templates.test.ts (current tests to extend)
    - .planning/phases/19-workflow-documentation/19-RESEARCH.md (Pattern 3: Self-Contained Workflow Document Structure, anti-patterns, error handling examples)
  </read_first>
  <behavior>
    - Test: scan template contains '## Prerequisites' section
    - Test: scan template contains '## Instructions' with numbered steps
    - Test: scan template contains '## Output Format' with exact formatting specification
    - Test: scan template contains '## Error Handling' with at least 3 error scenarios (CLI fails, no results, JSON parse error)
    - Test: scan template contains '## Edge Cases' section
    - Test: scan template contains 'allowed-tools:' in frontmatter
    - Test: scan template does NOT contain 'CLAUDE.md' as a dependency reference or preload instruction (self-containment)
    - Test: scan template contains a TEMPLATE_VERSION comment (e.g., '<!-- template-version: 2 -->')
  </behavior>
  <action>
    **TDD RED phase**: Add the following tests to the `generateScanCommand` describe block in `tests/unit/commands/templates.test.ts`:

    1. `it('contains ## Prerequisites section')` -- `expect(output).toContain('## Prerequisites')`
    2. `it('contains ## Instructions with step-by-step process')` -- `expect(output).toContain('## Instructions')` and `expect(output).toContain('### Step 1')`
    3. `it('contains ## Output Format with exact formatting rules')` -- `expect(output).toContain('## Output Format')`
    4. `it('contains ## Error Handling with CLI failure, no results, and parse error scenarios')` -- check for `## Error Handling`, `CLI Command Fails` or similar heading, `No Results` or `No Issues`, `JSON Parse Error` or `Parse Error`
    5. `it('contains ## Edge Cases section')` -- `expect(output).toContain('## Edge Cases')`
    6. `it('contains allowed-tools in frontmatter')` -- frontmatter contains `allowed-tools:`
    7. `it('is self-contained with no CLAUDE.md preload dependency')` -- `expect(output).not.toMatch(/preload.*CLAUDE\.md|load.*CLAUDE\.md|requires.*CLAUDE\.md/i)`
    8. `it('contains template version comment')` -- `expect(output).toMatch(/<!-- template-version: \d+ -->/)`

    Run tests, confirm all 8 new tests FAIL (RED).

    **TDD GREEN phase**: Rewrite `generateScanCommand()` in `src/commands/evolve-scan.ts` to produce a comprehensive workflow document. The function must:

    1. **Add `TEMPLATE_VERSION` constant**: `const SCAN_TEMPLATE_VERSION = '2';` at module level. Also export a getter: `export function getScanTemplateVersion(): string { return SCAN_TEMPLATE_VERSION; }`

    2. **Update frontmatter** to include:
       ```yaml
       name: scan
       description: Run a deep harness-evolve configuration scan to detect quality issues
       disable-model-invocation: true
       allowed-tools: Bash(npx harness-evolve *)
       ```

    3. **Add `<!-- template-version: ${SCAN_TEMPLATE_VERSION} -->` comment** immediately after frontmatter closing `---`.

    4. **Structure the body** with these sections (each section heading is mandatory):

       **# Evolve Scan** -- one-line purpose: "Run a deep scan of your Claude Code configuration to detect quality issues and optimization opportunities."

       **## Context** -- what this command does: analyzes CLAUDE.md, .claude/rules/, settings.json, .claude/commands/ for redundancy, mechanization gaps, staleness, conflicts, structure issues, hook redundancy, and command convention violations. Mention 7 scanners.

       **## Prerequisites** -- check that harness-evolve is installed:
       - Run `npx harness-evolve --version` to verify the tool is available
       - If command not found, tell user to install: `npm install -g harness-evolve` or `npx harness-evolve init`
       - If already installed, proceed to Instructions

       **## Instructions** -- numbered steps:
       - Step 1: Run the scan: `npx harness-evolve scan`
       - Step 2: Capture the JSON output (the CLI outputs JSON to stdout)
       - Step 3: Parse the JSON -- it contains `problems` array and `suggestions` array
       - Step 4: Present results using the Output Format section below

       **## Output Format** -- exact specification:
       - Start with a summary line: "Found X problem(s) and Y suggestion(s)."
       - If problems exist, show "### Problems (must fix)" section. For each problem, show as a bullet with: **PROBLEM** [CONFIDENCE] badge, description, affected file(s), and suggested fix with expected effect.
       - If suggestions exist, show "### Suggestions (optional improvements)" section. Same format but with **SUGGESTION** badge.
       - If zero problems, congratulate: "No problems detected -- your configuration is clean."
       - If suggestions exist alongside clean problems, mention them as optional.
       - End with: "Run `/evolve:apply` to review and apply recommendations interactively."
       - Use exact markdown formatting: `**PROBLEM** [HIGH]`, `**SUGGESTION** [MEDIUM]`, etc.

       **## Error Handling** -- three scenarios (per research Pattern 3):
       - **CLI Command Fails**: If `npx harness-evolve scan` exits with non-zero code or outputs an `error` field, show the error to user, suggest `harness-evolve status` to check installation, and if "not found" suggest `harness-evolve init`.
       - **No Results Found**: If scan returns zero recommendations (empty arrays), congratulate the user and suggest `harness-evolve status` for overall health.
       - **JSON Parse Error**: If output is not valid JSON, show raw output for debugging and suggest `npx harness-evolve@latest scan` for version mismatch.

       **## Edge Cases** -- specific situations:
       - Project has no `.claude/` directory: scan still runs but may find fewer issues. This is normal for new projects.
       - Multiple CLAUDE.md files in parent directories: scanner checks the project-level one; parent files are not in scope.
       - Large number of rules files (50+): scan may take a few seconds longer but will complete.

       **## Notes** -- cross-references:
       - Recommendations from this scan are stored and can be reviewed with `/evolve:apply`
       - Background analysis (via hooks) also generates recommendations on the same pipeline
       - Re-running scan replaces previous scan results

    Run tests, confirm all 8 new tests PASS (GREEN) plus all existing tests still pass.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/harness-evolve && npx vitest run tests/unit/commands/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/evolve-scan.ts contains `const SCAN_TEMPLATE_VERSION = '2'`
    - src/commands/evolve-scan.ts contains `export function getScanTemplateVersion()`
    - generateScanCommand() output contains `## Prerequisites`
    - generateScanCommand() output contains `## Instructions`
    - generateScanCommand() output contains `### Step 1`
    - generateScanCommand() output contains `## Output Format`
    - generateScanCommand() output contains `## Error Handling`
    - generateScanCommand() output contains `## Edge Cases`
    - generateScanCommand() output contains `## Notes`
    - generateScanCommand() output contains `allowed-tools:`
    - generateScanCommand() output contains `<!-- template-version:`
    - generateScanCommand() output does NOT contain preload/load CLAUDE.md dependency instructions
    - tests/unit/commands/templates.test.ts contains at least 8 new test cases for scan workflow
    - `npx vitest run tests/unit/commands/templates.test.ts` exits 0
  </acceptance_criteria>
  <done>Scan template generator produces a 100+ line comprehensive workflow document with prerequisites, step-by-step instructions, exact output format, error handling (3 scenarios), edge cases, and notes. Template version tracking constant exported. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite apply template generator with comprehensive workflow</name>
  <files>src/commands/evolve-apply.ts, tests/unit/commands/templates.test.ts</files>
  <read_first>
    - src/commands/evolve-apply.ts (current template to rewrite)
    - tests/unit/commands/templates.test.ts (tests updated in Task 1)
    - src/commands/evolve-scan.ts (Task 1's output -- reference for consistent style)
    - .planning/phases/19-workflow-documentation/19-RESEARCH.md (error handling examples, anti-patterns)
  </read_first>
  <behavior>
    - Test: apply template contains '## Prerequisites' section
    - Test: apply template contains '## Instructions' with numbered steps
    - Test: apply template contains '## Output Format' with exact formatting specification
    - Test: apply template contains '## Error Handling' with at least 3 error scenarios
    - Test: apply template contains '## Edge Cases' section
    - Test: apply template contains 'allowed-tools:' in frontmatter
    - Test: apply template does NOT contain CLAUDE.md preload dependency
    - Test: apply template contains a TEMPLATE_VERSION comment
  </behavior>
  <action>
    **TDD RED phase**: Add the following tests to the `generateApplyCommand` describe block in `tests/unit/commands/templates.test.ts`:

    1. `it('contains ## Prerequisites section')` -- `expect(output).toContain('## Prerequisites')`
    2. `it('contains ## Instructions with step-by-step process')` -- `expect(output).toContain('## Instructions')` and `expect(output).toContain('### Step 1')`
    3. `it('contains ## Output Format section')` -- `expect(output).toContain('## Output Format')`
    4. `it('contains ## Error Handling with multiple scenarios')` -- `## Error Handling` + at least 2 sub-scenarios
    5. `it('contains ## Edge Cases section')` -- `expect(output).toContain('## Edge Cases')`
    6. `it('contains allowed-tools in frontmatter')` -- frontmatter contains `allowed-tools:`
    7. `it('is self-contained with no CLAUDE.md preload dependency')` -- no preload/load CLAUDE.md references
    8. `it('contains template version comment')` -- matches `<!-- template-version: \d+ -->`

    Run tests, confirm 8 new tests FAIL (RED).

    **TDD GREEN phase**: Rewrite `generateApplyCommand()` in `src/commands/evolve-apply.ts` to produce a comprehensive workflow document. The function must:

    1. **Add `APPLY_TEMPLATE_VERSION` constant**: `const APPLY_TEMPLATE_VERSION = '2';` at module level. Also export: `export function getApplyTemplateVersion(): string { return APPLY_TEMPLATE_VERSION; }`

    2. **Update frontmatter** to include:
       ```yaml
       name: apply
       description: Review and apply pending harness-evolve recommendations one by one
       disable-model-invocation: true
       argument-hint: "[filter: all|high|medium|low]"
       allowed-tools: Bash(npx harness-evolve *)
       ```

    3. **Add `<!-- template-version: ${APPLY_TEMPLATE_VERSION} -->` comment** after frontmatter.

    4. **Structure the body** with these mandatory sections:

       **# Evolve Apply** -- one-line purpose: "Review pending harness-evolve recommendations interactively. Apply, skip, or dismiss each one."

       **## Context** -- what this command does: reads pending recommendations generated by `harness-evolve scan` or background analysis hooks. Presents each recommendation for user decision. Supports filtering by confidence level.

       **## Arguments** -- `$ARGUMENTS` handling:
       - If `$ARGUMENTS` is "high", "medium", or "low": filter to only that confidence level
       - If `$ARGUMENTS` is "all" or empty: show all pending recommendations
       - If `$ARGUMENTS` is unrecognized: show all and note the filter was ignored

       **## Prerequisites** -- check that harness-evolve is installed:
       - Run `npx harness-evolve --version` to verify
       - If not found, instruct: `npm install -g harness-evolve` or `npx harness-evolve init`

       **## Instructions** -- numbered steps:
       - Step 1: Read pending recommendations: `npx harness-evolve pending`
       - Step 2: If no pending recommendations, inform user and suggest `/evolve:scan`
       - Step 3: For each recommendation, present: severity badge (PROBLEM/SUGGESTION), confidence level (HIGH/MEDIUM/LOW), title, description, evidence, suggested action
       - Step 4: Ask user to choose: **Apply** (run `npx harness-evolve apply-one <id>`), **Skip** (leave pending), **Dismiss** (run `npx harness-evolve dismiss <id>`)
       - Step 5: After each action, report result and move to next
       - Step 6: After all processed, show summary (count applied, skipped, dismissed)

       **## Output Format** -- exact specification:
       - Present each recommendation in a consistent card format:
         ```
         **[SEVERITY] [CONFIDENCE]** Title
         Description text
         Evidence: evidence text
         Suggested: action text

         Choose: [Apply] [Skip] [Dismiss]
         ```
       - Summary at end: "Done: X applied, Y skipped, Z dismissed."
       - If all were skipped/dismissed, suggest running `/evolve:scan` for fresh analysis.

       **## Error Handling** -- scenarios:
       - **CLI Command Fails**: If `npx harness-evolve pending` fails, show error and suggest `harness-evolve status`
       - **Apply Fails**: If `npx harness-evolve apply-one <id>` returns error, show the error, do NOT retry automatically, ask user if they want to skip or dismiss instead
       - **Dismiss Fails**: If `npx harness-evolve dismiss <id>` returns error, show error and continue to next recommendation

       **## Edge Cases** -- specific situations:
       - Only 1 recommendation pending: still show the full card format, skip the "next recommendation" transition
       - User asks to stop mid-way: summarize what was processed so far and note remaining count
       - Recommendation references a file that no longer exists: show a warning but still allow apply/skip/dismiss
       - Filter returns zero matches: "No [level] recommendations pending. Run without filter to see all, or `/evolve:scan` for fresh analysis."

       **## Notes** -- cross-references:
       - Recommendations are generated by `/evolve:scan` or automatic background analysis hooks
       - Applied recommendations modify files (settings.json, CLAUDE.md, .claude/rules/, etc.)
       - Dismissed recommendations will not reappear until a new scan generates them
       - Skipped recommendations remain pending for the next `/evolve:apply` session

    Run tests, confirm all new tests PASS (GREEN) plus all existing tests still pass.

    **Final verification**: Run `npx vitest run tests/unit/commands/templates.test.ts` -- all tests green.
  </action>
  <verify>
    <automated>cd /Users/qinyuan/claude/r1ckyIn_GitHub/harness-evolve && npx vitest run tests/unit/commands/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/evolve-apply.ts contains `const APPLY_TEMPLATE_VERSION = '2'`
    - src/commands/evolve-apply.ts contains `export function getApplyTemplateVersion()`
    - generateApplyCommand() output contains `## Prerequisites`
    - generateApplyCommand() output contains `## Instructions`
    - generateApplyCommand() output contains `### Step 1`
    - generateApplyCommand() output contains `## Output Format`
    - generateApplyCommand() output contains `## Error Handling`
    - generateApplyCommand() output contains `## Edge Cases`
    - generateApplyCommand() output contains `## Notes`
    - generateApplyCommand() output contains `allowed-tools:`
    - generateApplyCommand() output contains `<!-- template-version:`
    - generateApplyCommand() output does NOT contain preload/load CLAUDE.md dependency instructions
    - tests/unit/commands/templates.test.ts contains at least 8 new test cases for apply workflow
    - `npx vitest run tests/unit/commands/templates.test.ts` exits 0
  </acceptance_criteria>
  <done>Apply template generator produces a 100+ line comprehensive workflow document with prerequisites, step-by-step instructions, exact output format, error handling (3 scenarios), edge cases, and notes. Template version tracking constant exported. All tests pass including scan tests from Task 1.</done>
</task>

</tasks>

<verification>
Run the full template test suite to confirm all tests pass:
```bash
npx vitest run tests/unit/commands/templates.test.ts
```

Run the full project test suite to confirm no regressions:
```bash
npx vitest run
```

Verify template generators produce 100+ lines each:
```bash
node -e "import('./dist/commands/evolve-scan.js').then(m => console.log(m.generateScanCommand().split('\n').length))"
```
</verification>

<success_criteria>
- Both template generators produce comprehensive workflow documents (100+ lines each)
- Each template has: Prerequisites, Instructions (with numbered steps), Output Format, Error Handling (3+ scenarios), Edge Cases, Notes
- Both templates include `allowed-tools: Bash(npx harness-evolve *)` in frontmatter
- Both templates include `<!-- template-version: N -->` comment for version tracking
- Both templates are fully self-contained (no CLAUDE.md preload dependency)
- Version getter functions exported: getScanTemplateVersion(), getApplyTemplateVersion()
- All existing tests still pass, 16+ new tests added
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/19-workflow-documentation/19-01-SUMMARY.md`
</output>
